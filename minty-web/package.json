{
    "name": "minty-web",
    "version": "0.0.0",
    "description": "Frontend code for Minty web server",
    "scripts": {
        "build": "node esbuild/build.mjs",
        "test": "echo \"Error: no test specified\" && exit 1",
        "watch": "node esbuild/watch.mjs"
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "htmx.org": "^2.0.2",
        "hyperscript.org": "^0.9.12"
    },
    "devDependencies": {
        "esbuild": "0.23.1"
    }
}
